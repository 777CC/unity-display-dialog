Problem
-Unity cannot set command line arguments to hide resolution dialog.

KIOS setting
-Set unity to hidden resolution dialog by default.
and use batch file to show show resolution dialog.

Requirement
-Unity window player
-EXE name must be same name with current directory name.  